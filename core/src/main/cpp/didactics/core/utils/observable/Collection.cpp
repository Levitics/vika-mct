
#include <didactics/core/utils/observable/Collection.hpp>

/*
   Collection::Collection() {
   }

   Collection::Collection(const Collection& orig) {
   }

   Collection::~Collection() {
   }


   template <typename ValueType>
   auto Collection<ValueType>::insert(ValueType && element)
   {
   //    auto const i = ++last_id_;
   //    auto n = std::make_unique<Collection::Node>();
   //    n->node_id = i;
   //    n->element = std::forward<ValueType>(element);
   }
 */
