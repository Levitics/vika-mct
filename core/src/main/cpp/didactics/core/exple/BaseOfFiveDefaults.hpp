
#ifndef BASEOFFIVEDEFAULTS_HPP
#define BASEOFFIVEDEFAULTS_HPP

class BaseOfFiveDefaults
{
public:

    BaseOfFiveDefaults();
    BaseOfFiveDefaults(const BaseOfFiveDefaults & orig);
    virtual ~BaseOfFiveDefaults();

private:

};

#endif
